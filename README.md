### 项目指令

```shell
# Install dependencies
npm install

# Start the project
npm run start

# Build the project
npm run build

# Format all files
npm run prettier

# Commit to Git
git add .
npm run commit
```
